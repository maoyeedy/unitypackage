# ADR 0002: Single CLI surface

Replaced separate `unitypackage-extractor` and `unitypackage-packer` binaries with one `unitypackage-tools` binary. Subcommands: extract, pack, inspect, verify, web. No legacy bin aliases.
