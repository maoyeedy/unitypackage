# P5 -- Cross-package API consistency [SHIPPED 2026-05-26]

Allowed case-insensitive GUIDs, supported preview thumbnail generation in `tryCreateUnityPackage`, and split `oversized-pathname` diagnostic into separate 200-char path and 100-byte tar limit checks.
