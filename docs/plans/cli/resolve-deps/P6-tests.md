### P6 -- Tests  [DONE 2026-05-27]

Shipped: 34 new tests across 4 files (guidScanner +8, pathnameIndex +7, dependencyResolver +9, pack.test.ts +7 integration). Extended `NO_REFERENCE` set with `.cg`/`.bytes`/`.meta`; symlink + AbortSignal + stats invariant coverage; depth-chain + overlapping-roots + binary-YAML resolver tests; `--resolve-deps` CLI integration via dry-run JSON and verify pass.
