{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -qE '(/dist/|\\.tsbuildinfo$|/assets/web/)' && echo 'BLOCKED: do not edit generated files (dist/, .tsbuildinfo, assets/web/)' >&2 && exit 2; exit 0"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/maoyeedy/Repos/unitypackage-workspace/unitypackage && bun run lint 2>&1 | tail -30"
          }
        ]
      }
    ]
  }
}
